import React from 'react';
import { Character } from '../interfaces/character';

export function NewCharButton({party, editParty}: 
    {party: Array<Character>, editParty: (newParty: Array<Character>) => void}):
    JSX.Element{
    
    return(
        <></>
    );
}